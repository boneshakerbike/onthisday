import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Suggestions | 8i11' };

export default function SuggestionsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
